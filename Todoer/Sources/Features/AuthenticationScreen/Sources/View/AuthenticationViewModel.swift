import Foundation

extension Authentication.Reducer {
    // MARK: - ViewModel

    /// ViewModel structure for the Authentication screen
    /// This structure is designed to run on the main actor to ensure UI updates are thread-safe
    @MainActor
    struct ViewModel {}
}
